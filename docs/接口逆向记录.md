# 接口逆向记录（取证版）

> 本文档记录通过**真实网络探测**得到的站点行为证据，用于替代「照搬参考仓库」。
> 全部探测均为**只读**请求（GET 页面、查询类接口、登录态检查），
> **未发起任何选课 / 退课写请求**。
> 凭证通过环境变量传入临时脚本，未写入仓库。

---

## 一、已**实测确认**的事实

### 1. 端点基路径正确

以下接口返回 **HTTP 200 + application/json**，证明基路径
`http://bkxk.szu.edu.cn/xsxkapp/sys/xsxkapp/` 正确：

| 接口 | 实测结果 | 鉴权要求 |
| --- | --- | --- |
| `POST /xsxkapp/sys/xsxkapp/elective/batch.do` | 200 JSON，返回选课批次（`typeCode:"02"`、`tacticName:"可选可退"`、`beginTime:"2026-09-24 14:00:00"`、`schoolTerm:"2026-2027-1"`） | 公开 |
| `POST /xsxkapp/sys/xsxkapp/publicinfo/sysparam.do` | 200 JSON，返回系统参数（`schoolTerm`、各 `displayName*` 等） | 公开 |
| `POST /xsxkapp/sys/xsxkapp/student/check/login.do` | 200 JSON，`{"code":"#E2140600091","msg":"认证失败"}` | 需登录 |
| `POST /xsxkapp/sys/xsxkapp/elective/volunteered.do` | **401** HTML 错误页 | 需登录 |
| `GET /xsxkapp/sys/xsxkapp/*default/grablessons.do` | **401** HTML 错误页 | 需登录 |

### 2. 登录态失效时的服务器行为（重要）

| 请求形态 | 未登录时的响应 |
| --- | --- |
| 普通请求（无 `X-Requested-With`） | **302 → `http://bkxk.szu.edu.cn/xsxkapp/sys/xsxkapp/*default/index.do`**（页面 HTML，长度 18876） |
| 带 `X-Requested-With: XMLHttpRequest` | **401** + HTML 错误页（2267 字节，`<title>401</title>`） |

> **结论**：解析接口响应不能只看状态码。必须
> （a）禁用自动重定向并识别 3xx；（b）带 `X-Requested-With` 以获得明确的 401；
> （c）对 200 但返回 HTML 的情况也判定为登录态失效。
> 代码已据此实现 `api_client.NotAuthenticatedError`。

### 3. token 的传递位置（**推翻/确认参考仓库写法**）

从 `http://xkres.szu.edu.cn/fe_components/bh_utils.js` 取到 `doAjax` 真实签名：

```js
BH_UTILS.doAjax = function (url, params, method, requestOption, headers) {
    var ajaxOptions = $.extend({}, {
        type: method || 'POST',
        url: url,
        data: params || {},
        headers: headers || {},   // ← 第 5 个参数直接作为 jQuery ajax headers
        dataType: 'json',
```

而 `indexBS.min.js` 中的调用：

```js
function queryVolunteered(e){
  var t=(new Date).getTime();
  return BH_UTILS.doAjax(
      BaseUrl+"/sys/xsxkapp/elective/volunteered.do?timestamp="+t,
      e, "post", {}, {token: sessionStorage.token});
}
```

**结论**：
- 接口调用的 token 走 **HTTP 请求头 `token`**（参考仓库写法正确）；
- **每个接口 URL 都附加 `?timestamp=<13位毫秒>`**（参考仓库只给 `courseResult.do` 加了）；
- 与之相对，**页面跳转**的 token 走 **URL query**（见下节）——
  这正好印证需求里「不是所有请求都带 token，部分页面 URL 会自动拼接 token」。

### 4. 「跳转选课网页」必须带 token（用户反馈已实测印证）

`index.min.js` 原文：

```js
var e = sessionStorage.token, t = "";
"01" == m ? t = BaseUrl + "/sys/xsxkapp/*default/curriculavariable.do?token=" + e
          : "02" == m && (t = BaseUrl + "/sys/xsxkapp/*default/grablessons.do?token=" + e),
window.location.href = t
```

`batch.do` 返回的批次 `typeCode` 为 `"02"`（选课），因此当前应进入
**`grablessons.do?token=<token>`**。新开浏览器标签页没有 `sessionStorage`，
所以只能用 URL 传参还原登录上下文 —— 与用户描述完全一致。

代码实现：`api_client.build_elective_page_url(credentials)`。

### 5. 首页重定向行为

`GET http://bkxk.szu.edu.cn/` → **302** → `.../*default/index.do`；
带查询串（如 `/?token=...`）时直接 200。首页 `index.do` 对任何访客都返回 200
（SPA 外壳，长度 18876），**因此 200 不能作为已登录的判据**。

### 6. 站点实际使用的其它端点（从 JS 中提取）

```
/sys/xsxkapp/elective/batch.do            /sys/xsxkapp/elective/batchisopen.do
/sys/xsxkapp/elective/volunteered.do      /sys/xsxkapp/publicinfo.do
/sys/xsxkapp/publicinfo/dictionary.do     /sys/xsxkapp/publicinfo/onlineUsers.do
/sys/xsxkapp/publicinfo/sysparam.do       /sys/xsxkapp/student/4/vcode.do
/sys/xsxkapp/student/check/login.do       /sys/xsxkapp/student/vcode/image.do
/sys/xsxkapp/student/register.do          /sys/xsxkapp/student/logout.do
/sys/xsxkapp/student/auth/logout.do       /sys/xsxkapp/*default/content.do
/sys/xsxkapp/*default/curriculavariable.do
/sys/xsxkapp/*default/grablessons.do      ← 选课子页面
/sys/xsxkapp/*default/publicinfo.do
```

### 7. 课程查询失败的确切原因（**三次提交的凭证均判定为未登录**）

需求文档「测试预置参数」先后提供了 **3 组** cookie/token，实测结果一致：

| 接口 | 返回 |
| --- | --- |
| `POST student/check/login.do` | `{"code":"#E2140600091","msg":"认证失败"}` |
| `POST elective/programCourse.do` | `{"code":"302","msg":"未查询到登录信息"}` |
| `POST elective/recommendedCourse.do` | `{"code":"302","msg":"未查询到登录信息"}` |
| `POST elective/courseResult.do` | `{"code":"302","msg":"未查询到登录信息"}` |
| `POST elective/volunteered.do` | `{"code":"302","msg":"未查询到登录信息"}` |
| `GET *default/grablessons.do` | 401 |

**结论**：`code=302 / msg=未查询到登录信息` 就是「课程查询失败」的直接原因 ——
服务器没有识别出登录会话，与查询参数无关（已用 5 组不同 `querySetting`
参数组合验证，报错完全相同）。

**可疑点（重要）**：3 组凭证中的
`JSESSIONID=DA340811F6FA94D837703289A24B7334` 与
`route=4b012a35482600f90be1abcead8af5f6` **完全一致、从未变化**，
只有 `_WEU` 每次不同。而 `_WEU` 是统一身份认证侧的 cookie，
**xsxkapp 的会话标识是 `JSESSIONID`** —— 它没变，说明复制的很可能是
DevTools 里同一条**过期请求**的 cookie，而非当前活跃会话。

### 8. 未登录时服务器返回的两种形态（都已适配）

| 请求形态 | 未登录响应 |
| --- | --- |
| 普通请求 | `302 → *default/index.do`（HTML 首页） |
| 带 `X-Requested-With: XMLHttpRequest` | `401` + HTML 错误页；**接口路径**则返回 JSON `{"code":"302","msg":"未查询到登录信息"}` |

程序已把这三种表现统一归一为 `api_client.NotAuthenticatedError`，
并把服务器原始 `code`/`msg` 一并展示在日志与界面状态栏。

## 二、**尚未验证**（受限于凭证仍未生效）

由于上述登录态问题，以下内容仍待**有效凭证**到位后验证：

因此以下内容仍待新鲜凭证到位后验证：

1. `programCourse.do` / `recommendedCourse.do` 的真实响应字段名
   → 决定 `course_model._FIELD_CANDIDATES`（课程总号 / 课程性质 / 开课单位 / 学分 /
   是否 MOOC / 课程时间 目前均为**低置信度推测**）；
2. 课程查询确切的 `querySetting` 字段与分页规则；
3. **收藏接口**（参考仓库缺失，选课子应用的 JS 位于 `grablessons.do` 之后，
   需登录后才能取到）—— 见 `TODO.md`；
4. 选课写接口 `volunteer.do` 的真实报文（按红线**仅记录、不提交**）。

## 三、复现方式

仓库内提供只读探测工具 `tools/probe_api.py`，凭证由环境变量传入、不落盘：

```powershell
$env:SZU_COOKIE = "<从浏览器复制的完整 cookie>"
$env:SZU_TOKEN  = "<sessionStorage.token>"
$env:SZU_STU    = "<学号>"
$env:SZU_BATCH  = "<electiveBatchCode>"
.\.venv\Scripts\python.exe tools\probe_api.py
```

该工具**只调用登录检查与查询类接口**，不含任何写接口调用。
